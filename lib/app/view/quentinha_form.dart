import 'package:flutter/material.dart';

class QuentinhaForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Cadastro de Quentinha'),),
    );
  }
}